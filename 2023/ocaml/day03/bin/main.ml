open Core

let get_total_for_part_nums s = 
  let symbol_set = Set.

let () =
  let input = In_channel.read_all "input.txt" in
  let summed = get_total_for_part_nums input in
  printf "Sum all lines: %d\n" summed
